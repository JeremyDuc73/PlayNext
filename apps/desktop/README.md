# @playnext/desktop

Application Windows PlayNext (Tauri 2 + React + TypeScript).

```bash
# depuis la racine du monorepo
npm run dev:ui        # UI seule (Vite)
npm run dev:desktop   # shell Tauri natif
```

Commandes Rust autorisées (allowlist) : `get_app_info`, `ping_native`.
